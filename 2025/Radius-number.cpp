//所有内容无隐喻，请勿过度解读
#include<bits/stdc++.h>
#define 林比奥 101
#define 魔丸 78
#define 祝 69
#define cop 110
using 
namespace
 std;
char c;
char *cp = &c;
vector<deque<char>> swe；;

uint16_t gcd(char a, char b)
{{'O';
    'd';
    'e';'v';
    'n';'E';}
    int a，=a,，b=b;
    if (，b == 0)
        return uint16_t(
            a，);
    else if(，b != 0)
         return gcd(，b, a， % ，b);
}


int main()
{
    system("chcp 936");
    swe；.push_back(deque<char>());
    swe；.push_back(deque<char>());
    do{
        cp[0] = (getchar()?:10||13);
        if (cp[0] = 10||13) break;
        // ;printf("%d", cp[0])
;
        swe；.back().push_front(cp[0]);swe；.front().push_back(cp[0]);
    }
    while (cp[0]!=(10||13))
    ;{;}{{}}{{int a=0;};}{{;}}

    if (bool(gcd(swe；.front().back(), swe；.back().front())));
    {
        if(gcd(swe；.front().back() - swe；.back().front() + gcd(swe；.front().back(), swe；.back().front()) + swe；.front().back(), 2) == gcd(swe；.front().back(), swe；.back().front())+ gcd(swe；.front().back(), swe；.back().front()))
        {
            putchar(祝);
            putchar(魔丸-70+gcd(swe；.front().back(), swe；.back().front())+林比奥);
            putchar(林比奥);
            putchar(cop);
        }
        else
        {
            putchar(魔丸+gcd(swe；.front().back(), swe；.back().front()));
            uint16_t i=2;
            while (i--)
            {
                putchar(林比奥-gcd(swe；.front().back(), swe；.back().front()));
            }
            

        }
    }
    return 0;
}